---
name: pvs-component-decrement-bible
description: The governing truth for PVS feeder/component consumption — per-lot total usage is the bible; board count drives the decrement; HMI sync upkeeps it; lot-end verifies; exhaust learning calibrates.
metadata: 
  node_type: memory
  type: project
  originSessionId: 19fcfab6-cf20-4ed1-99bb-c8d8f04306be
  modified: 2026-09-06T11:00:35.362Z
---

**The per-lot total component usage is the BIBLE behind every component decrement** (Danial, 2026-09-01). The whole feeder-consumption system is built around this single governing number.

**The truth hierarchy (top governs):**
1. **Bible — per-lot total usage** = `MountedPerBoard × lot board count`, per feeder/component. This is the authority the running decrement must reconcile to. At lot completion, total consumed for a component MUST equal this. (`MountedPerBoard` = mount points per board × panel factor.)
2. **Board count drives the decrement** — each machine decrements ITS OWN feeders off ITS OWN board-out (R0) signal, never M4-for-all (L3 JUKI/M1 rides M2). `remaining = start − MountedPerBoard × boardsApplied`.
3. **HMI manual sync upkeeps the board count** — PVS CANNOT read the machine counter over serial while producing (Appendix F: C1M/C1Z answer A4E00 during AUTO production — see [[pvs-c1m-machine-counter]]). So the operator keys each machine's Sony HMI count (in **PANELS**) and PVS corrects that machine's feeders by the difference. Mid-line machines (M1–M3) need no badge; the PCB-out/last machine (M4) is supervisor-gated (its count is the lot count's basis). On the monitor page; M4 is corrected from the lot card on the verify page instead.
4. **Lot-completion verify** — at lot end (before the machine reset destroys the counts), PVS compares each machine's board tally to the lot count and flags any component whose usage deviates from the bible. `/api/lot/usagecheck` live; written to `lot-usage/<lot>.json`; audited as `LotUsageCheck`.
5. **Exhaust learning (shadow)** — the actual parts-out is ground truth: if a reel empties earlier/later than predicted, that drift is the tell-tale the per-board count is off. PVS learns a per-part EWMA correction (`ExhaustCalibration`, `/api/calibration`) — SHADOW only, never rewrites a reel count.

**Hard invariants:**
- **Never lose a reel's count.** All learning/verification is READ-ONLY; reel balances change only by the board-out decrement, a genuine parts-out, or an operator/HMI correction — all reversible/audited.
- Board tally is **seeded to the lot's board count on restart/re-baseline** (`SeedBoardsApplied`), else a post-restart HMI sync would double-decrement the restored feeders.
- HMI shows **panels (PWBs)**, not boards — sync sends `panels` to `/api/lot/adopt` and `/api/machine/synccount`.

Built 2026-09-01, deployed all 5 lines. Files: `MachineInventory` (BoardsApplied/SyncToBoardCount/SeedBoardsApplied/ReelUsage), `ExhaustCalibration`, `SessionCoordinator` (SyncMachineBoardCountAsync/LotUsageCheck/RecordExhaustCalibration). Related: [[pvs-feeder-consumption-c1z]], [[pvs-count-two-counters-and-cap]], [[pvs-c1m-machine-counter]].

**UNITS — nailed 2026-09-06 (Danial):** lot size / PO target = **BOARDS** (`DeliveryDocuments.RevisedQty|Quantity`, via `GetLotTargetAsync`). The machine counts **PANELS** — one R0/board-complete = one panel. `_m4PanelsTotal`, `_boardsApplied`, `SyncToBoardCount` all work in **panels/cycles**. perPanel (boards/panel): **L261=6, L264=6; all others (L254/L307/L309/L311/L313/L347)=4** (`PanelBoards` config, same on all lines). Decrement per R0 = **feeder-list count (`f.Qty`) × panels** (`SessionCoordinator` ~line 1641 `perBoard = f.Qty × panels`; `OnBoardComplete` subtracts it once per R0) — this is CORRECT. Mount comes from the **feeder list, NOT ProductBOM** (Danial: "use feeder list count, don't touch bom"). Acceptance test: used-per-part per lot == lot size(boards) × per-board mount.

**LOT-END CALIBRATION vs LOT SIZE — L1 canary 2026-09-05 (commit a4c5b35, branch session-2026-09-03):** the lot-usage recalc now calibrates to the **lot size (target)** instead of PVS's own drifted panel count (which can't calibrate itself). UNITS: converts lot size boards → panels via `÷ perPanel` before `SyncToBoardCount` (the panel factor then cancels: MountedPerBoard × (lotSize/perPanel) = f.Qty × lotSize). Only machines beyond `UsageToleranceOverPct` are corrected (accurate reels left alone). Deployed **L1 ONLY** as canary; NOT yet validated (needs a lot to finalize). **Calibration-source decision OPEN:** target = plan → ~0.4% error on over/under-run; C1M-as-truth NOT viable (0 C1M reads land, machines refuse A4E00, M4 rejects it); tightest = operator-confirmed produced count.

**TWO DRIFT CAUSES (2026-09-06):** (1) **missed R0s** → board count short → under-decrement → phantom remaining → fixed by lot-end calibration to lot size. (2) **UNSCANNED REEL SWAP** → operator changes reel without scanning → PVS tracks the wrong reel → the big L5 drift (VS1-9540 family, VV5-3000-000 showed 9,872 phantom after 20 boards). These are distinct from pickup **attrition** (VC−TC, the C1Z axis — "not perfected, don't touch for counting"). Designed (not built) an **unscanned-reel watchdog**: trigger on machine-parts-out-while-PVS-remaining-high OR PVS-remaining-~0-but-still-picking → WhatsApp supervisor + mark feeder untrusted until rescan. See [[pvs-reel-uid-whole-number]].

**L1 CANARY 2026-09-06 — calibration FIRED, defect found + FIXED (`ece6eb9`):** lot HC20799111000 (1500 boards = 375 panels) finalized; the recalc snapped each machine's `BoardsApplied` to 375 (sane: M4's pre-reset C1Z = 1504–1505 attempts on 4-mount feeders = 376 panels). BUT `RefreshLotAsync` reset only the lot anchor, never the per-machine tally, so the next lot read 459–465 (375+90) → 410% deviation / −1476 pcs per feeder in `/api/lot/usagecheck`; an HMI sync or the next lot-end recalc would have handed ~375 panels × mount BACK to every feeder. Fix: `SeedBoardsApplied(0)` on lot/side change + force-end (feeders untouched). Danial first said "everything is working, do not change anything", then after the previous session owned the bug: "go ahead, fix and deploy to L1". Deployed L1 only 19:27 while idle; verified tallies = 90 = lot panels, 0 gap, reel remaining unchanged. L2–L5 have the same latent carry-over but it's masked (inventory refresh at startup/check reseeds the tally) — they get the fix with the calibration rollout. Next check: L1's HC20799536000 finalize (300 panels).

**MACHINE-TALLY SYNC BUILT 2026-09-06 (`48f7e44`, L1 SHADOW via `/api/setup` tallySync; results at `/api/tallysync`):** Danial: "the feeder should be decreased based on the machine counter" + rule "at lot end the machine count and lot count are checked, the decrement is calibrated, and confirmed at parts-exhaust". Live decrement per R0 stays; every landed C1Z auto-syncs the machine's board tally (median successful ÷ mount, stale pre-reset reports rejected, |delta|<2 ignored) through the HMI-sync path — shadow records only; apply corrects. Original idea: per machine, median over reset-aligned feeders of `VC ÷ mount-per-panel` gives the machine's own panel count (M4: 376 vs target 375) — tighter than the PO target for lot-end calibration, and it uses the pickup report only for the tally, NOT for consumption/attrition (so it respects "don't touch C1Z for counting"). Caveats: intermittent landing (M1 landed none all lot), feeders not reset with the lot skew it (M2 F113 = 2128), M2's machine clock reads 2003.
